import React, { FunctionComponent } from "react";

const LayoutAppWrapper: FunctionComponent = ({ children }) => (
	<div className="m-4 font-jdcode">{children}</div>
);

export default LayoutAppWrapper;
